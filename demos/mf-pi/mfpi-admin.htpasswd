# Default /usermanagement/ Basic Auth credentials (admin / mf@pass2026).
# run-nginx.sh bind-mounts a generated file over this path; ADMIN_USER /
# ADMIN_PASSWORD override the defaults. nginx skips '#' comment lines here
# (verified against this image).
admin:$apr1$RxGJErrO$LNPqRxt/aKEl5qcQcW1bz.
